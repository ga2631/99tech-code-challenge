# Server Configuration
PORT=3000
NODE_ENV=development
API_PREFIX=/api/v1

# Database Configuration
# Use :memory: for in-memory DB or a path to SQLite file (e.g. data/database.sqlite)
DB_PATH=data/database.sqlite

# Security & CORS
CORS_ORIGIN=*
